from . import views
from . import models
def getGroupInfo(list):
        dic={''}
        group=map(func,list)

def getKeysToDelete(keys):
        print('getKeysToDelete')
        print(keys)
        '''idUser=models.useraccount.objects.get(key=keys).pk
        userGruop=models.user_in_group.objects.get(user=idUser)
        if not views.leavGruop(userGruop)['state']:
                return ({'state':False})
        '''
def double(bonus):
        print('sosososo')
        return bonus * 2



